import { StyleSheet } from 'react-native';
import styled from 'styled-components/native';

export const styles = StyleSheet.create({
  verticalSpacing: {
    paddingTop: 10,
    paddingBottom: 24,
    paddingHorizontal: 24,
  },
});

export const WeatherListScreenStyles = {
  Wrapper: styled.View`
    flex: 1;
  `,
};
